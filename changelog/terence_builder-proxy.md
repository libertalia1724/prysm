### Added

- Optional `proxy` in proposer settings builder config to decouple the VC-signed builder URL from the HTTP dial target, enabling builder sidecars, and renamed `relays` to `builders` with the old name kept as a deprecated alias.
